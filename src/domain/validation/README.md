# Validation Tests

- auth.test.ts — Functions: isValidEmail/validatePassword/passwordsMatch; Flows: trimming + case normalization, password strength rules (length, uppercase, number, special), equality checks.
